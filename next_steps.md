## Next Steps:

- Check how to query JSONB fields in Postgres using SQLAlchemy ORM.
- Implement the missing methods in the models for CRUD operations. ???
- Check how often the sabdab db is updated, and create a DAG to update our local sabdab db accordingly.
- Check if my sabdab db is the only data from sabdab
- Should I create a separate database table for the 'researchers', the 'company', and the 'project'?